public void reverse(LinkedList l){
  ListNode temp = l.head;
  while (temp != null) {
    ListNode foo = temp.next;
    ListNode bar = foo.next;
    foo.next = temp;
    temp = foo;
  }
  head = temp;
}
